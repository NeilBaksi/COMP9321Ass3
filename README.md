# COMP9321Ass3
Design and Development of a Data Analytics Service

To Run Webapp:
    `cd client`
   ` npm install`
    `npm start `

To Run API:
    `cd server`
    `pip3 install -r requirements.txt`
    `python3 api.py`